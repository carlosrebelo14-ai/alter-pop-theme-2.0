{%- comment -%}
  Alterpop — customer order page (wireframe 4b). Standalone: this theme has no
  customer-account area yet (see CLAUDE.md "no account area" pending item).
  Reachable via the order-status "View order details" link and the order
  confirmation email (?key= token authenticates without a login).
{%- endcomment -%}
{% section 'main-order' %}
