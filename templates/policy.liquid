{%- comment -%}
  Alterpop — native store policy pages (/policies/*). 'policy' does not
  support JSON templates, so this is a Liquid template that renders the
  shared legal section. See sections/main-policy.liquid.
{%- endcomment -%}
{% section 'main-policy' %}
